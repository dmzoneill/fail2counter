# fail2counter configuration
# Copy to .env and fill in your values: cp .env.example .env

# --- Required ---

# Email address for scan reports
NOTIFICATION_EMAIL=you@example.com

# AI Provider (set ONE of these)
# Option A: Direct Anthropic API
ANTHROPIC_API_KEY=sk-ant-xxxxx

# Option B: Google Vertex AI
#ANTHROPIC_VERTEX_PROJECT_ID=my-gcp-project
#ANTHROPIC_VERTEX_REGION=us-east5
#GOOGLE_APPLICATION_CREDENTIALS=/etc/fail2counter/gcp-credentials.json

# --- Optional ---

# Email sender address (default: fail2counter@localhost)
#NOTIFICATION_FROM=fail2counter@yourdomain.com

# SMTP server for sending reports (default: localhost)
#SMTP_HOST=localhost
#SMTP_PORT=25

# Override Claude model (default: auto-detected)
#ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
#ANTHROPIC_VERTEX_MODEL=claude-sonnet-4@20250514

# PostgreSQL connection (default: local socket as postgres user)
#FAIL2COUNTER_DSN=host=/var/run/postgresql dbname=fail2counter user=postgres

# VPN rotation interval - rotate exit IP every N scans (default: 30)
#VPN_ROTATE_INTERVAL=30
